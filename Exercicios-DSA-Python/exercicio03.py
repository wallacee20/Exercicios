# Exercício 3 - Crie uma tupla de 4 elementos, multiplique cada elemento da tupla
# por 2 e guarde os resultados em uma 
# lista
elementos = (14, 15, 20)
lista = []
for i in elementos:
    lista.append(i * 2)# nova_lista = i * 2
                       # lista.append(nova_lista)
print(lista)
print(type(lista))